export interface Translation {
    id: string;
    text: string;
    sourceLang: string;
    targetLang: string;
    translatedText: string;
  }
  
  export interface Transcription {
    id: string;
    transcription: string;
  }
  